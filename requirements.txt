# Infrastructure SDK Python Dependencies
# Simplified EC2-based Windows VM management without Kubernetes complexity
# Direct AWS EC2 management for faster deployment and reduced dependencies

# Core async and data handling
asyncio-throttle==1.0.2
aiofiles==23.2.1
pydantic==2.5.3
dataclasses-json==0.6.3

# AWS SDK for Python (Primary dependency for EC2 management)
boto3==1.34.34
botocore==1.34.34
aiobotocore==2.11.2

# Configuration management
PyYAML==6.0.1
python-dotenv==1.0.0

# HTTP clients for API interactions
httpx==0.26.0
aiohttp==3.9.1

# Data validation and serialization
marshmallow==3.20.2
marshmallow-dataclass==8.6.0

# Logging and monitoring (CloudWatch integration)
structlog==23.2.0
boto3-stubs[logs,cloudwatch]==1.34.34
watchtower==3.0.1  # CloudWatch log handler

# Metrics and monitoring
prometheus-client==0.19.0

# Date and time handling
python-dateutil==2.8.2
pytz==2023.4

# Cryptography and security (for RDP password generation)
cryptography==41.0.8
bcrypt==4.1.2

# Windows RDP client utilities (optional)
# For programmatic RDP connection management
paramiko==3.4.0  # SSH tunneling if needed

# Development and testing dependencies
# (These would typically be in requirements-dev.txt in production)
pytest==7.4.4
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.12.0
black==23.12.1
flake8==7.0.0
mypy==1.8.0
isort==5.13.2

# Type hints and stubs
types-PyYAML==6.0.12.12
types-python-dateutil==2.8.19.14
types-requests==2.31.0.10
boto3-stubs[essential]==1.34.34

# Optional dependencies for enhanced functionality

# Redis for caching and session state (optional)
# Useful for multi-instance deployments
redis==5.0.1
aioredis==2.0.1

# Database for persistent VM state (optional)
# Recommended for production deployments
asyncpg==0.29.0
sqlalchemy==2.0.25
alembic==1.13.1

# Cost optimization and AWS pricing analysis
pandas==2.1.4
numpy==1.26.2

# AWS Cost Explorer API (optional)
# For advanced cost analysis and optimization
aws-cost-explorer==0.1.0

# Performance monitoring (optional)
psutil==5.9.6  # System metrics
GPUtil==1.4.0  # GPU monitoring (if using GPU instances)

# Enterprise integration (optional)
# For Active Directory integration
ldap3==2.9.1
python-ldap==3.4.3

# Email notifications for cost alerts (optional)
emails==0.6

# Simplified deployment utilities
click==8.1.7  # CLI utilities
rich==13.7.0  # Rich terminal output
typer==0.9.0  # Modern CLI framework

# Documentation (optional)
# mkdocs==1.5.3
# mkdocs-material==9.5.3

# REMOVED DEPENDENCIES (Kubernetes/KubeVirt):
# kubernetes==28.1.0
# kubernetes-asyncio==24.2.3
# kubevirt-python-client (not needed for direct EC2)

# ADDED BENEFITS:
# - Reduced installation size (no Kubernetes dependencies)
# - Faster container builds
# - Simpler deployment (no cluster required)  
# - Direct AWS integration
# - Better cost optimization with native AWS APIs